<div class="taxonomy container">
  <nav class="breadcrumb" aria-label="面包屑导航">
    <a href="<%- url_for('/') %>">首页</a>
    <span class="breadcrumb__sep">›</span>
    <span class="breadcrumb__current">标签</span>
  </nav>

  <h1 class="taxonomy__title">
    <span class="taxonomy__icon">#</span> 所有标签
  </h1>
  <p class="taxonomy__count">共 <%= site.tags.length %> 个标签</p>

  <section class="taxonomy-all">
    <div class="tag-cloud">
      <% site.tags.sort('name').forEach(function(tag) { %>
        <a href="<%- url_for(tag.path) %>" class="tag-chip">
          #<%= tag.name %> <span class="tag-count"><%= tag.length %></span>
        </a>
      <% }) %>
    </div>
  </section>
</div>
